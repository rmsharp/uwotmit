# Run Package Check

Run a full R CMD check on the uwotmit package.

```bash
cd /Users/rmsharp/Documents/R_packages/uwotmit && Rscript -e "devtools::check()"
```

After running, report the check results including any NOTEs, WARNINGs, or ERRORs.
