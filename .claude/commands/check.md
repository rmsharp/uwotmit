# Run Package Check

Run a full R CMD check on the uwotlite package.

```bash
cd /Users/rmsharp/Documents/R_packages/uwotlite && Rscript -e "devtools::check()"
```

After running, report the check results including any NOTEs, WARNINGs, or ERRORs.
